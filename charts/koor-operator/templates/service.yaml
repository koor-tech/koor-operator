apiVersion: v1
kind: Service
metadata:
  name: {{ include "koor-operator.fullname" . }}
  labels:
    {{- include "koor-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "koor-operator.selectorLabels" . | nindent 4 }}
